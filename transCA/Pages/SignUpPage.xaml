<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="transCA.Pages.SignUpPage"
    NavigationPage.HasNavigationBar="True"
    NavigationPage.HasBackButton="True"
    Style="{StaticResource contentStyle}"
    >

    <ScrollView>
        <StackLayout Padding="20" Style="{StaticResource stackStyle}">
            <Label Text="Sign Up" Style="{StaticResource labelStyle}"/>
            <Entry x:Name="UserNameEntry" Placeholder="User Name" Keyboard="Default" Style="{StaticResource entryStyle}"/>
            <Entry x:Name="EmailEntry" Placeholder="Email" Keyboard="Email" Style="{StaticResource entryStyle}"/>
            <Entry x:Name="PasswordEntry" Placeholder="Password" IsPassword="True" Keyboard="Default" Style="{StaticResource entryStyle}"/>
            <Entry x:Name="ConfirmPasswordEntry" Placeholder="Confirm Password" IsPassword="True" Keyboard="Default" Style="{StaticResource entryStyle}"/>
            <Button x:Name="LoginButton" Text="Sign Up" Clicked="LoginButton_Clicked" Style="{StaticResource buttonStyle}"/>


        </StackLayout>

    </ScrollView>
</ContentPage>
